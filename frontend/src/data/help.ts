export const BFS_INFO = `
<h3>BFS — поиск в ширину (Breadth-First Search)</h3>
<p>
<b>BFS</b> — алгоритм обхода графа, который исследует вершины
<b>по уровням</b>, начиная с заданной стартовой вершины.
</p>
<p>
Сначала посещаются все соседние вершины, затем соседи соседей и т.д.
</p>
<ul>
  <li>Использует <b>очередь</b></li>
  <li>Находит <b>кратчайшие пути</b> в невзвешенном графе</li>
  <li>Работает для ориентированных и неориентированных графов</li>
</ul>
<p><b>Применения:</b> поиск кратчайшего пути, проверка связности, поиск компонент.</p>
`;

export const DFS_INFO = `
<h3>DFS — поиск в глубину (Depth-First Search)</h3>
<p>
<b>DFS</b> — алгоритм обхода графа, который углубляется
<b>как можно дальше</b> по одному пути, прежде чем вернуться назад.
</p>
<p>
Алгоритм идёт в глубину, пока это возможно, и только потом
возвращается к предыдущим вершинам.
</p>
<ul>
  <li>Использует <b>стек</b> или <b>рекурсию</b></li>
  <li>Не гарантирует кратчайший путь</li>
  <li>Хорош для анализа структуры графа</li>
</ul>
<p><b>Применения:</b> поиск циклов, топологическая сортировка, компоненты связности.</p>
`;

export const REVERSE_GRAPH_INFO = `
<h3>Обращение графа (Reverse Graph)</h3>

<p>
<strong>Обращение графа</strong> — это операция, при которой у <em>ориентированного графа</em>
меняется направление всех рёбер на противоположное.
</p>

<p>
Если в исходном графе есть ребро <code>A → B</code>, то в обращённом графе
будет ребро <code>B → A</code> с тем же весом.
</p>

<h4>Зачем используется обращение графа?</h4>
<ul>
  <li>Поиск компонент сильной связности (алгоритм Косарайю)</li>
  <li>Анализ достижимости вершин</li>
  <li>Работа с зависимостями и потоками управления</li>
</ul>

<h4>Свойства</h4>
<ul>
  <li>Количество вершин не меняется</li>
  <li>Количество рёбер не меняется</li>
  <li>Меняется только направление рёбер</li>
</ul>

<h4>Пример</h4>
<p>Исходный граф:</p>
<pre>
1 → 2
2 → 3
</pre>

<p>Обращённый граф:</p>
<pre>
2 → 1
3 → 2
</pre>

<p>
Обращение графа применяется только к <strong>ориентированным графам</strong>.
Для неориентированного графа результат будет тем же самым графом.
</p>
`;

export const CYCLES_INFO = `
<h3>Циклы в графе</h3>

<p>
<b>Цикл</b> в графе — это путь, который начинается и заканчивается в одной и той же вершине,
при этом проходит хотя бы по одному ребру.
</p>

<h4>Виды циклов</h4>

<ul>
  <li>
    <b>Простой цикл</b> — вершины (кроме первой и последней) не повторяются.
  </li>
  <li>
    <b>Петля</b> — ребро, которое ведёт из вершины в саму себя.
  </li>
  <li>
    <b>Отрицательный цикл</b> — цикл, сумма весов рёбер которого меньше нуля.
  </li>
</ul>

<h4>Циклы в ориентированном и неориентированном графе</h4>

<p>
В <b>ориентированном графе</b> цикл существует, если можно вернуться в исходную вершину,
двигаясь строго по направлениям рёбер.
</p>

<p>
В <b>неориентированном графе</b> цикл — это любой замкнутый путь без повторения рёбер.
</p>

<h4>Зачем искать циклы</h4>

<ul>
  <li>Проверка графа на ацикличность</li>
  <li>Поиск ошибок в зависимостях</li>
  <li>Обнаружение бесконечного улучшения (отрицательные циклы)</li>
  <li>Анализ маршрутов и сетей</li>
</ul>

<h4>Как находят циклы</h4>

<ul>
  <li>
    <b>DFS</b> — обнаружение обратного ребра
  </li>
  <li>
    <b>Алгоритм Беллмана–Форда</b> — поиск отрицательных циклов
  </li>
</ul>

<p>
Если в графе есть отрицательный цикл, задача поиска кратчайшего пути может не иметь решения.
</p>
`;

export const ECCENTRICITY_INFO = `
<h3>Эксцентриситет, радиус и центр графа</h3>

<p>
<strong>Эксцентриситет вершины</strong> — это максимальное расстояние
от данной вершины до любой другой вершины графа.
</p>

<p>
Проще говоря, эксцентриситет показывает,
насколько далеко самая удалённая вершина от выбранной.
</p>

<pre>
e(v) = max(dist(v, u)), где u — любая вершина графа
</pre>

<hr />

<p>
<strong>Радиус графа</strong> — это минимальный эксцентриситет среди всех вершин.
</p>

<pre>
R = min(e(v))
</pre>

<p>
Радиус показывает, насколько "компактным" является граф.
</p>

<hr />

<p>
<strong>Центр графа</strong> — это множество вершин,
эксцентриситет которых равен радиусу графа.
</p>

<p>
Именно из центра графа расстояние до самых дальних вершин минимально.
</p>

<hr />

<p>
Эти характеристики используются при анализе сетей,
маршрутизации, оптимальном размещении серверов
и поиске наиболее "удобных" вершин.
</p>
`;

export const DEGREE_INFO = `
<h3>Степень вершины</h3>

<p>
<strong>Степень вершины</strong> — это количество рёбер, инцидентных данной вершине.
Иными словами, это число рёбер, которые «связаны» с вершиной.
</p>

<h4>В неориентированном графе</h4>

<p>
В <strong>неориентированном графе</strong> степень вершины равна количеству рёбер,
выходящих из неё (или входящих в неё — разницы нет).
</p>

<p>
Петля (ребро из вершины в саму себя) увеличивает степень вершины на <strong>2</strong>,
так как считается дважды.
</p>

<p>
Обозначение степени вершины <em>v</em>: <strong>deg(v)</strong>.
</p>

<h4>В ориентированном графе</h4>

<p>
В <strong>ориентированном графе</strong> вводятся понятия <strong>полустепеней</strong>:
</p>

<ul>
  <li>
    <strong>Полустепень захода</strong> (<em>in-degree</em>) — количество рёбер,
    входящих в вершину.
  </li>
  <li>
    <strong>Полустепень исхода</strong> (<em>out-degree</em>) — количество рёбер,
    выходящих из вершины.
  </li>
</ul>

<p>
Полная степень вершины в ориентированном графе равна сумме полустепеней:
</p>

<p>
<strong>deg(v) = in-degree(v) + out-degree(v)</strong>
</p>

<h4>Пример</h4>

<p>
Если в ориентированном графе в вершину <em>A</em> входят 3 ребра и из неё выходят 2,
то:
</p>

<ul>
  <li>in-degree(A) = 3</li>
  <li>out-degree(A) = 2</li>
  <li>deg(A) = 5</li>
</ul>

<h4>Зачем это нужно</h4>

<p>
Степень вершины и полустепени используются для:
</p>

<ul>
  <li>анализа структуры графа;</li>
  <li>поиска источников и стоков в ориентированных графах;</li>
  <li>анализа сетей (социальных, транспортных, потоковых);</li>
  <li>проверки свойств графа и алгоритмов.</li>
</ul>
`;

export const MST_KRUSKAL_INFO = `
<h3>Остовное дерево</h3>
<p>
<b>Остовное дерево</b> неориентированного связного графа — это подграф,
который содержит <b>все вершины</b> исходного графа, является <b>деревом</b>
(не содержит циклов) и имеет <b>минимально возможное число рёбер</b>.
</p>

<p>
Если граф взвешенный, то часто рассматривают <b>минимальное остовное дерево (MST)</b> —
остовное дерево с минимальной суммарной стоимостью (весом) рёбер.
</p>

<hr />

<h3>Алгоритм Краскала</h3>
<p>
<b>Алгоритм Краскала</b> — жадный алгоритм построения минимального остовного дерева
во взвешенном неориентированном графе.
</p>

<p><b>Идея алгоритма:</b></p>
<ol>
  <li>Отсортировать все рёбра графа по возрастанию веса.</li>
  <li>Последовательно добавлять рёбра в остов,
      если их добавление <b>не образует цикл</b>.</li>
  <li>Остановиться, когда в остове будет <code>|V| - 1</code> рёбер.</li>
</ol>

<p>
Для проверки появления цикла обычно используется структура данных
<b>система непересекающихся множеств</b> (Union-Find).
</p>

<p><b>Свойства алгоритма:</b></p>
<ul>
  <li>Работает только для <b>неориентированных</b> графов</li>
  <li>Гарантирует минимальный суммарный вес остова</li>
  <li>Имеет сложность <code>O(E log E)</code></li>
</ul>
`;

export const UNREACHABLE_INFO = `
<h3>Недостижимые вершины графа</h3>

<p>
Недостижимыми называются вершины, в которые <b>нельзя попасть</b> из заданной
начальной вершины, следуя рёбрам графа.
</p>

<h4>Идея задачи</h4>
<p>
Задача поиска недостижимых вершин сводится к проверке достижимости:
какие вершины были посещены при обходе графа, а какие — нет.
</p>

<h4>Как искать недостижимые вершины</h4>
<p>
Алгоритм выглядит следующим образом:
</p>

<ol>
  <li>Выбрать начальную вершину.</li>
  <li>Выполнить обход графа (<b>BFS</b> или <b>DFS</b>).</li>
  <li>Отметить все посещённые вершины.</li>
  <li>Все вершины, которые не были посещены, считаются недостижимыми.</li>
</ol>

<h4>Направленный и ненаправленный граф</h4>
<ul>
  <li>
    В <b>ненаправленном графе</b> недостижимые вершины появляются,
    если граф состоит из нескольких компонент связности.
  </li>
  <li>
    В <b>направленном графе</b> достижимость зависит от направления рёбер,
    поэтому недостижимые вершины могут существовать даже в связном графе.
  </li>
</ul>

<h4>Результат</h4>
<p>
Результатом алгоритма является список вершин, в которые невозможно попасть
из заданной стартовой вершины.
</p>

<p>
Поиск недостижимых вершин используется для анализа связности,
проверки корректности маршрутов и анализа структуры графа.
</p>
`;

export const DIJKSTRA_INFO = `
<h3>Алгоритм Дейкстры</h3>

<p>
<strong>Алгоритм Дейкстры</strong> — это алгоритм поиска кратчайших путей
от одной стартовой вершины до всех остальных вершин взвешенного графа.
</p>

<p>
Алгоритм работает <strong>только для графов с неотрицательными весами рёбер</strong>.
Если в графе есть отрицательные веса, результат будет некорректным.
</p>

<h4>Идея алгоритма</h4>
<ul>
  <li>Для каждой вершины хранится текущее лучшее расстояние от источника</li>
  <li>На каждом шаге выбирается вершина с минимальным известным расстоянием</li>
  <li>Через неё происходит попытка улучшить расстояния до соседей</li>
</ul>

<h4>Основные шаги</h4>
<ol>
  <li>Задать расстояние до стартовой вершины равным 0, до остальных — бесконечности</li>
  <li>Выбрать непосещённую вершину с минимальным расстоянием</li>
  <li>Обновить расстояния до всех её соседей</li>
  <li>Пометить вершину как обработанную</li>
  <li>Повторять, пока есть непосещённые вершины</li>
</ol>

<h4>Результат работы</h4>
<p>
Алгоритм находит:
</p>
<ul>
  <li>длину кратчайшего пути до каждой вершины</li>
  <li>при хранении предков — сами кратчайшие пути</li>
</ul>

<h4>Сложность</h4>
<ul>
  <li>O(V²) — при простой реализации</li>
  <li>O((V + E) log V) — при использовании очереди с приоритетом</li>
</ul>

<h4>Применение</h4>
<ul>
  <li>Навигация и карты</li>
  <li>Маршрутизация в сетях</li>
  <li>Поиск оптимальных путей</li>
</ul>
`;

export const BELLMAN_FORD_INFO = `
<h3>Алгоритм Беллмана—Форда</h3>

<p>
<b>Алгоритм Беллмана—Форда</b> — это алгоритм поиска кратчайших путей
от одной вершины до всех остальных в взвешенном графе.
В отличие от алгоритма Дейкстры, он <b>допускает отрицательные веса рёбер</b>.
</p>

<h4>Идея алгоритма</h4>
<p>
Алгоритм последовательно <b>релаксирует</b> (уточняет) расстояния по всем рёбрам графа.
Если после достаточного количества итераций расстояние всё ещё уменьшается,
значит в графе существует <b>цикл отрицательного веса</b>.
</p>

<h4>Основные шаги</h4>
<ol>
  <li>Инициализировать расстояние до стартовой вершины как 0, до остальных — как бесконечность.</li>
  <li>Повторить <code>V - 1</code> раз (где <code>V</code> — число вершин):
    <ul>
      <li>для каждого ребра попытаться улучшить расстояние до его конца.</li>
    </ul>
  </li>
  <li>Выполнить ещё одну итерацию:
    <ul>
      <li>если расстояние уменьшается — найден отрицательный цикл.</li>
    </ul>
  </li>
</ol>

<h4>Отрицательные циклы</h4>
<p>
Если существует путь, по которому суммарный вес рёбер отрицателен,
то кратчайший путь не определён — его можно уменьшать бесконечно.
Беллман—Форд умеет <b>обнаруживать такие циклы</b>.
</p>

<h4>Сложность</h4>
<ul>
  <li>Временная: <b>O(V · E)</b></li>
  <li>Пространственная: <b>O(V)</b></li>
</ul>

<h4>Когда использовать</h4>
<ul>
  <li>Когда в графе есть рёбра с отрицательным весом</li>
  <li>Когда нужно проверить наличие отрицательных циклов</li>
  <li>Для учебных и аналитических задач</li>
</ul>
`;

export const FLOYD_WARSHALL_INFO = `
<h2>Алгоритм Флойда–Уоршелла</h2>
<p>Алгоритм Флойда–Уоршелла используется для поиска кратчайших путей между <strong>всеми парами вершин</strong> взвешенного графа (как ориентированного, так и неориентированного).</p>

<h3>Основная идея:</h3>
<p>Идея алгоритма заключается в последовательном рассмотрении всех вершин как промежуточных и обновлении расстояний между каждой парой вершин, если путь через промежуточную вершину короче.</p>

<h3>Пошаговый процесс:</h3>
<ol>
  <li>Создать матрицу расстояний <code>dist[i][j]</code>, где хранится минимальное известное расстояние от вершины <code>i</code> до вершины <code>j</code>. Изначально:
    <ul>
      <li><code>dist[i][i] = 0</code> для всех вершин</li>
      <li><code>dist[i][j] = вес ребра (i,j)</code> если ребро существует</li>
      <li><code>dist[i][j] = ∞</code>, если ребро отсутствует</li>
    </ul>
  </li>
  <li>Для каждой вершины <code>k</code> (в качестве промежуточной) обновляем матрицу:
    <pre>dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])</pre>
  </li>
  <li>После завершения всех шагов <code>dist[i][j]</code> содержит длину кратчайшего пути между вершинами <code>i</code> и <code>j</code>.</li>
</ol>

<h3>Сложность:</h3>
<p>Временная сложность: <code>O(V^3)</code>, где <code>V</code> — количество вершин.</p>
<p>Пространственная сложность: <code>O(V^2)</code> для хранения матрицы расстояний.</p>

<h3>Особенности:</h3>
<ul>
  <li>Работает с графами с отрицательными весами ребер (но не с отрицательными циклами).</li>
  <li>Подходит для плотных графов, где нужно знать расстояния между всеми парами вершин.</li>
</ul>
`;

export const GOLDBERG_RAO_INFO = `
<h2>Алгоритм Голдберга–Рао</h2>
<p>Алгоритм Голдберга–Рао предназначен для нахождения <strong>максимального потока</strong> в ориентированном графе. Он является улучшением метода блокирующих потоков (по аналогии с алгоритмом Диница) с использованием <strong>дельта-графа</strong>.</p>

<h3>Основная идея:</h3>
<ul>
  <li>Использует <strong>дельта-граф</strong>, где выделяются ребра с пропускной способностью не меньше некоторой дельта-цели.</li>
  <li>На каждом шаге алгоритм находит блокирующие потоки в дельта-графе и увеличивает поток на этих ребрах.</li>
  <li>С течением алгоритма значение дельты уменьшается, что позволяет постепенно добиваться точного максимального потока.</li>
</ul>

<h3>Пошаговый процесс:</h3>
<ol>
  <li>Инициализация потока нулями и установка начальной дельты (например, степенной грани для максимальной пропускной способности).</li>
  <li>Построение дельта-графа — оставляются только ребра с пропускной способностью ≥ delta.</li>
  <li>В дельта-графе ищутся блокирующие потоки и поток увеличивается.</li>
  <li>Дельта уменьшается (обычно делится на 2) и шаги повторяются до delta = 1.</li>
</ol>

<h3>Сложность:</h3>
<p>Временная сложность: <code>O(min(V^{2/3}, E^{1/2}) * E * log(U))</code>, где <code>V</code> — число вершин, <code>E</code> — число рёбер, <code>U</code> — максимальная пропускная способность ребра. Это лучше, чем классический алгоритм Форда–Фалкерсона для больших графов.</p>

<h3>Особенности:</h3>
<ul>
  <li>Эффективен для больших плотных графов с высокой пропускной способностью ребер.</li>
  <li>Использует концепцию дельта-графа для ускорения нахождения блокирующих потоков.</li>
  <li>Гарантирует точный максимальный поток после окончания всех шагов с уменьшающейся дельтой.</li>
</ul>
`;
